package com.example.demo.society.method;

import com.example.demo.entity.Success;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Successinterface extends JpaRepository<Success, Integer> {
}
